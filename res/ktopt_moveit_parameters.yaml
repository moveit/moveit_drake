ktopt_interface:
  num_iterations: {
    type: int,
    description: "dummy",
    default_value: 1000,
    validation: {
      gt_eq<>: [1]
    }
  }
  control_points: {
    type: int,
    description: "Number of control points used to represent the B-Spline",
    default_value: 10
  }
